meta {
  name: graphtest
  type: http
  seq: 1
}

post {
  url: http://localhost:8085/graphql
  body: json
  auth: inherit
}

body:json {
  {
    "query": "{ allCourses { id name category schedule } allStudents { id name address university { id name } } }"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
